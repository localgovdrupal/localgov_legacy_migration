id: localgov_events

label: 'LocalGov Events'

description: 'Targets Event related entities.'

source_type: 'Drupal 8 site'

shared_configuration:
  dependencies:
    enforced:
      module:
      - localgov_events
      - bhcc_events_plus
      - localgov_legacy_migration
  source:
    key: migration

dependencies:
  enforced:
    module:
      - localgov_events
      - localgov_legacy_migration
      - bhcc_events_plus
